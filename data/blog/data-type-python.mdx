---
title: 'Python data type'
date: '5/9/2022, 12:14:47 PM'
tags: ['python', 'basic']
draft: false
summary: 'Data types in Python programming language'
thumbnails: true
layout: PostLayout
---

A data type is a medium or memory on a computer that is used to store information.

Python itself has a data type that is quite unique when we compare it with other programming languages.

Here are the data types of the Python programming language:

| Data Type   | Example                  | Explanation                                                                             |
| ----------- | ------------------------ | --------------------------------------------------------------------------------------- |
| Boolean     | `True` or `False`        | Declare true `True` with value `1`, or false `False` with value `0`                     |
| Strings     | `"Let's learn Python"`   | Declare characters/sentences that can be letters, numbers, etc. (flanked by `"` or `'`) |
| Integer     | `25` or `1209`           | Declare an integer                                                                      |
| float       | `3.14` or `0.99`         | Declare a number that has a comma                                                       |
| Hexadecimal | `9a` or `1d3`            | Express numbers in hex format (base 16 numbers)                                         |
| Complex     | `1 + 5h `                | Expressing pairs of real and imaginary numbers                                          |
| List        | `['xyz', 786, 2.23]`     | Data strings that store various data types and their contents can be changed            |
| tuples      | `('xyz', 768, 2.23)`     | Data strings that store various data types but whose contents cannot be changed         |
| Dictionary  | `{'name': 'adi','id':2}` | Data string that stores various data types in the form of pointer and value pairs       |

To try various data types, please try the Python script below.

```python:data-type.py
#boolean datatype
#There are 2 types of boolean data, namely true or false
print(true)
print(false)

#String datatype
#begins and ends with double quotes " " or single quote ' '
print("Let's learn Python")
print('Learn Python Very Easy')

#Integer data type
#integers
print(20)

#float datatype
#decimal number
print(3.14)

#Hexadecimal data type
#hexadecinal numbers
print(9a)

#Complex data types
#complex number
print(5h)

#List datatype
# begins and ends with square brackets [ ]
print([1,2,3,4,5])
print(["one", "two", "three"])

#Tuple datatype
# begins and ends with parentheses ( )
print((1,2,3,4,5))
print(("one", "two", "three"))

#Dictionary data types
#begins and ends with curly braces { }
print({"name":"Budi", 'age':20})

#Dictionary data type is entered into the biodata variable
biodata = {"name":"Andi", 'age':21}

#biography variable initialization process
print(biodata) #printing process of biodata variable which contains data type Dictionary
print(type(biodata)) # function to check the type of data type.  will show <class 'dict'> which means dict is a data type dictionary
```
